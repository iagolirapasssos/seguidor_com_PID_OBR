#include "Arduino.h"
#include "config.h"
#include "acao.h"

     
void loop(){
  //Serial.println(lerSensoresLinha());
  curvas();
  //PID(kP, kI, kD, PWM, media);
 //lerSensoresCor();
 //LedsAlerta(4, 150);
}
